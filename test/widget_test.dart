import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App placeholder test', (WidgetTester tester) async {
    // Placeholder — real tests require Supabase mocking.
    expect(1 + 1, equals(2));
  });
}
